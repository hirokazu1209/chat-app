<div class="message">
  <div class="upper-message">
    <div class="message-user">
      <!-- 投稿したユーザー名情報を出力する -->
      <%# messagesテーブルのuser_idに紐付いているユーザー名を、user.nameと記述することで取得 %>
      <%= message.user.name %>
    </div>
    <div class="message-date">
      <!-- 投稿した時刻を出力する -->
      <%= l message.created_at %>      
      <%# messagesテーブルのcreated_atカラムから、投稿した時刻を取得 %>
      <%= message.created_at %>
    </div>
  </div>
  <div class="lower-message">
    <div class="message-content">
      <!-- 投稿したメッセージ内容を記述する -->
      <%# messagesテーブルのcontentカラムから、メッセージの内容を取得 %>
      <%= message.content %>
    </div>
    <%# image_tagメソッドを記述して、画像を表示 %>
    <%# 要素の生成に条件をつけ、attached?メソッドで画像の添付をチェック %>
    <%# variantメソッドを使用することで、ファイルの表示サイズを指定 %>
    <%= image_tag message.image.variant(resize: '500x500'), class: 'message-image' if message.image.attached? %>
  </div>
</div>
